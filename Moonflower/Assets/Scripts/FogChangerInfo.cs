using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FogChangerInfo : MonoBehaviour
{
    // Start is called before the first frame update
    public bool anaiLavaTriggered = false;
    public bool mimbiLavaTriggered = false;
    public bool anaiMazeTriggered = false;
    public bool mimbiMazeTriggered = false;
    public bool isNormal = true;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
